
#include <stdio.h>

int main()
{
  int a=60,b=60,c=60;
   int sum = a+b+c;
  if(sum==180){
      printf("Valid triangle");
  }
  else{
      printf("not valid triangle");
  }
    return 0;
}